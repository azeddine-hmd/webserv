
namespace ws {
    namespace defaults {

        /*
         * config defaults
         */
        char const* CONFIG_PATH = "config/default.conf";
        //TODO: continue
        char const* CONFIG_ROOT = "/";

    } // namespace defaults
} // namespace ws