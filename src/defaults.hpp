
namespace ws {
    namespace defaults {

        /*
         * config defaults
         */
        char const* CONFIG_PATH = "config/default.conf";
        char const* CONFIG_ROOT = "~/www";
        //TODO: add more defaults

    } // namespace defaults
} // namespace ws